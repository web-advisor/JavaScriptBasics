function stringRepeat(str, num) {
    let repeatedString = "";
    while (num > 0) {
        stringRepeat = stringRepeat.concat(str);
        num--;
    }
    return stringRepeat;
}

function repeatString(str, num) {
    return str.repeaat(num);
}

console.log("Amar", 3);
console.log("Akhi", 5);